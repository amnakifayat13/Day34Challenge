/* Calculate and log the absolute difference between the number -5 and 5.*/
function absoluteDifferenceNum(num1, num2) {
    return Math.abs(num1 - num2);
}
console.log(absoluteDifferenceNum(-5, 5));
